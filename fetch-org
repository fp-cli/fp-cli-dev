#!/usr/bin/env bash

REPOLIST=$(curl --silent https://api.github.com/orgs/fp-cli/repos\?per_page=100 -q | jq '.[].name')

for NAME in $REPOLIST; do
	REPO=$(echo $NAME | tr -d '"')
    if [ ! -d "$REPO" ]; then
		git clone "https://github.com/fp-cli/$REPO"
	fi
done
